import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#f3f4f9",
      light: "#182027",
      dark: "#233044",
    },
  },
});

export default theme;
